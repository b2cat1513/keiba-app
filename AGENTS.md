# AGENTS.md — ジェニーAI予想 開発指示書

## 1. プロジェクト概要

- プロジェクト名：ジェニーAI予想
- GitHubリポジトリ：`b2cat1513/keiba-app`
- ブランチ：`main`
- 主な対象ファイル：`keiba_app.py`
- Streamlitアプリ：`keiba-app001.streamlit.app`
- 現在の開発版：Ver1.19系
- `COURSE_MASTER` は `keiba_app.py` 内にハードコードされており、実行時にExcelから読み込まない。

## 2. 最重要ルール

1. 既存機能を壊さない。
2. 依頼されていない予想ロジック・評価ロジック・重み・係数は変更しない。
3. 3F処理の修正では、現在正常に取得できているデータを壊さない。
4. 大きな書き換えをする前に、現在のデータの流れを確認する。
5. 推測だけで修正せず、実際の入力データと現在のコードを照合する。
6. 修正後は必ず構文チェックを行う。
7. 可能なら3Fパーサーを独立した関数としてテストできる形にする。
8. 変更したファイル、変更内容、テスト結果、未確認事項を明確に報告する。

## 3. 現在正常に動作している機能

以下は壊さないこと。

- スマホ入力／PC入力の切り替え
- Netkeiba／ウマニティ／競馬ラボ／画像OCRの入力方式
- 予想結果の訂正・削除UI
- 既存の予想ロジック
- 既存のコースデータ
- その他、現在動作しているUI・入力・表示機能

## 4. 現在の最重要課題：過去5走の上がり3F取得

過去5走データから各馬の上がり3Fを取得し、最大5走分を表示して平均値を計算する。

### 必須仕様

- `S` だけを対象にしない。
- `S / M / H` のいずれでも、その行にある3F値を正しく取得する。
- 日付をレース行の識別キーとして扱い、同じ日付の重複行を除外する。
- 1頭につき最大5走。
- 馬Aの3Fが馬Bに混入しないこと。
- 馬名・馬セクションとの対応を正しく維持する。
- ページやスクリーンショットの重複コピーがあっても、同じレースを二重計上しない。
- 3Fの取得・重複排除・平均計算を、可能な限り明確に分離する。
- race_id/date → horse → 3F の対応関係を追跡できるようにする。

## 5. 実際の入力データ（重要）

### #1 ロブチェン

入力例：

- 26/5/31 2:22.7 33.2 S
- 26/4/19 1:56.5 34.2 S
- 26/2/15 1:45.5 33.4 S
- 25/12/27 2:01.0 34.5 S
- 25/11/9 2:04.5 35.4 S

期待値：

`33.2 / 34.2 / 33.4 / 34.5 / 35.4`

平均：

`34.14`

### #2 エイシンインアウト

入力例：

- 26/8/23 1:59.8 33.9 S
- 26/7/5 2:01.4 35.0 S
- 26/6/21 1:48.5 35.1 M
- 26/2/21 1:56.1 37.9 M
- 26/2/1 1:48.2 38.5 H

期待値：

`33.9 / 35.0 / 35.1 / 37.9 / 38.5`

平均：

`36.08`

特に重要：

#2では1～3走目が正しく取得できているため、「3Fの数値を認識できない」ことだけが原因とは考えない。
4～5走目が別の値に置き換わるため、馬セクションの境界、行の対応付け、重複除去後の再マッピング、または別馬データとの混入を重点的に追跡すること。

## 6. 最新の実機テスト結果

直近テストでは次の結果になった。

- #1 ロブチェン
  - `33.2 / 34.2 / 33.4 / 34.5 / 35.4`
  - 平均 `34.14`
  - 正常。ここは現在の実装で壊さない。

- #2 エイシンインアウト
  - 実際：`33.9 / 35.0 / 35.1 / 35.0 / 34.5`
  - 期待：`33.9 / 35.0 / 35.1 / 37.9 / 38.5`
  - 平均も不正確。
  - 1～3走目は正常、4～5走目が誤って別データになっている。
  - 最重要の診断材料。

- #3 ゴーラックキー
  - 実際：`34.4 / 34.3 / 34.4 / 33.6`
  - 4走分しか取得されていない。

- #4 アルトラムス
  - 実際：`33.8 / 33.9`
  - 2走分しか取得されていない。

- #5 カムアップローゼス
  - 実際：`34.9 / 35.5`
  - 2走分しか取得されていない。

- #6 エムズビギン
  - 3Fデータなし。

- #7 コンジェスタス
  - 実際：`35.8 / 33.5 / 34.3 / 33.6`
  - 4走分しか取得されていない。

- #10 カフジエメンタール
  - 実際：`33.8 / 33.9`
  - 2走分しか取得されていない。

- #11 ショウナンハヤナミ
  - 実際：`33.9`
  - 1走分のみ取得。

画面上では「過去5走取り込み成功：9頭」と表示されたが、各馬の走数が不完全なケースが残っている。

## 7. 3F問題の調査方針

今回の問題では、最初から全パーサーを書き直さない。

まず以下のデータフローをコード全体で追跡する。

`文字入力`
→ `馬名の認識`
→ `馬ごとのセクション分割`
→ `各レース行の認識`
→ `日付・走破時計・3F・S/M/Hの抽出`
→ `日付による重複排除`
→ `各馬への再マッピング`
→ `最大5走への制限`
→ `平均計算`
→ `画面表示`

特に以下を重点調査する。

- 馬セクションの開始・終了判定
- 馬名とレース行の関連付け
- 全文を横断して3F数値だけを拾う処理が残っていないか
- `fallback_horse`、`horse_gate_map`、その他の暫定馬名・馬番マッピングが存在する場合、その役割
- 日付重複排除後に別馬へデータが再割当てされていないか
- 複数ページ／重複コピーを処理する際の状態管理
- #2の4・5走目がどこで `37.9 / 38.5` から `35.0 / 34.5` に変わるか

※上記の関数名や変数名が実際のコードに存在するとは限らない。存在する場合だけ調査し、実際のコードに基づいて判断すること。

## 8. Codexの最初の作業

最初のタスクではコードを変更しない。

以下を調査して報告する。

1. `keiba_app.py` 全体の構成
2. 主要関数
3. 入力UIと入力データの流れ
4. 3F処理の関数・処理箇所
5. 馬名認識の処理
6. 予想ロジックの処理
7. 予想結果の訂正・削除処理
8. 3Fデータが画面表示されるまでのデータフロー
9. 今回の#2の誤取得が発生する可能性がある箇所
10. 修正方針を2～3案提示する
11. 各案の既存機能への影響を説明する

この分析タスクではコード変更・ファイル削除・予想ロジック変更を行わない。

## 9. テスト要件

最低限、次を確認する。

### Test A
#1 ロブチェン

入力5走：
`33.2 / 34.2 / 33.4 / 34.5 / 35.4`

期待：
5走すべて取得、平均34.14。

### Test B
#2 エイシンインアウト

入力5走：
`33.9 / 35.0 / 35.1 / 37.9 / 38.5`

期待：
5走すべて取得、平均36.08。

### Test C
S/M/H混在

例：
`33.9 S / 35.1 M / 37.9 M / 38.5 H`

期待：
すべて3Fとして取得。

### Test D
同じ日付の重複行

同一レースが2回入力されても1回だけ計上。

### Test E
複数馬

馬Aの3Fと馬Bの3Fが混ざらない。

## 10. 検証コマンド

少なくとも以下を実行する。

`python -m py_compile keiba_app.py`

3Fパーサーを独立関数化した場合は、その関数に対するテストも実行する。

Streamlitでの実機確認が必要な場合は、コード上のテストと実機確認を分けて報告する。

## 11. 修正後の報告形式

必ず次を報告する。

### 変更ファイル
- ファイル名

### 変更内容
- 何を変更したか
- なぜ変更したか

### 3Fテスト結果
- #1 ロブチェン
- #2 エイシンインアウト
- S/M/H混在
- 重複日付
- 複数馬

### 構文・テスト
- 実行したコマンド
- 結果

### 未確認事項
- Streamlit実機でユーザー確認が必要なもの

## 12. 開発方針

「とりあえず動く正規表現を追加する」ことを繰り返さない。

今回#1が正常になったことは重要な前進なので、その動作を維持しながら#2以降の馬セクション境界と3Fデータ対応付けを調査する。

既存の予想ロジックには手を入れず、まず3F取り込み部分だけを安定させる。

不明点がある場合は推測で変更せず、コード上の根拠を示してから修正する。
